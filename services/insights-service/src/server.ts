import { env, listen, positiveIntegerEnv, secretEnv } from "@echo/service-core"; import { createInsightsApp } from "./app.js"; const port = positiveIntegerEnv("PORT", 4207); listen(createInsightsApp({ serviceToken: secretEnv("INSIGHTS_SERVICE_TOKEN"), userServiceToken: secretEnv("USER_SERVICE_TOKEN"), journalServiceToken: secretEnv("JOURNAL_SERVICE_TOKEN"), userUrl: env("USER_SERVICE_URL"), journalUrl: env("JOURNAL_SERVICE_URL"), timeoutMs: positiveIntegerEnv("REQUEST_TIMEOUT_MS", 5000) }), { name: "insights-service", port });
