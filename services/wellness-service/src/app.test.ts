import request from "supertest"; import { describe, expect, it } from "vitest"; import { createWellnessApp } from "./app.js"; import { hasUrgentLanguage } from "./wellness.service.js"; const service = new Proxy({}, { get: () => async () => [] }) as any; describe("wellness service", () => { it("has independent health", async () => expect((await request(createWellnessApp(service, { serviceToken: "w".repeat(32), userServiceToken: "u".repeat(32), userServiceUrl: "http://user", timeoutMs: 100 })).get("/health")).status).toBe(200)); it.each(["kill myself", "end my life", "suicide plan", "want to die", "hurt myself"])("aligns Buddy urgent-language detection for %s", (phrase) => expect(hasUrgentLanguage(`I ${phrase} tonight`)).toBe(true)); it("does not flag non-urgent reflection", () => expect(hasUrgentLanguage("I felt disconnected today")).toBe(false)); });
