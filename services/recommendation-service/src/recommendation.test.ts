import { describe, expect, it } from "vitest"; import { recommend } from "./recommendation.js"; describe("recommendations", () => { it("prioritizes urgent support", () => expect(recommend({ severity: "minimal", urgentLanguageDetected: true }).category).toBe("urgent_support")); it("provides CBT reflection for moderate screening", () => expect(recommend({ severity: "moderate", urgentLanguageDetected: false }).category).toBe("cbt_reflection")); });
