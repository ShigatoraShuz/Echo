import { Router } from "express"; import { createServiceApp, requireGatewayUser, requireInternalToken, sendData, ServiceError } from "@echo/service-core"; import { z } from "zod"; import { recommend } from "./recommendation.js"; const schema = z.object({ severity: z.enum(["minimal","mild","moderate","moderately_severe","severe"]), urgentLanguageDetected: z.boolean() }); const handler = (req: any, res: any, next: any) => { try { const parsed = schema.safeParse(req.body); if (!parsed.success) throw new ServiceError(400, "VALIDATION_ERROR", "Recommendation input is invalid."); sendData(res, recommend(parsed.data), req.requestId); } catch (error) { next(error); } }; export function createRecommendationApp(token: string) { const router = Router(); router.post("/recommendations", requireGatewayUser(token), handler); router.post("/internal/recommendations", requireInternalToken(token), handler); return createServiceApp({ name: "recommendation-service", router }); }
