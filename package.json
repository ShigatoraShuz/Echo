{
  "name": "echo",
  "version": "1.0.0-beta",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "packages/contracts"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev -w frontend\" \"npm run dev -w backend\"",
    "build": "npm run build -w @echo/contracts && npm run build -w frontend && npm run build -w backend",
    "lint": "npm run lint -w frontend && npm run lint -w backend",
    "typecheck": "npm run typecheck -w frontend && npm run typecheck -w backend",
    "check:auth-policy": "node scripts/check-auth-password-policy.mjs",
    "check:contracts": "npm run build -w @echo/contracts",
    "types:server:local": "node scripts/generate-server-types.mjs",
    "test:sql:isolated": "node backend/scripts/validate-analysis-sql.mjs",
    "test": "npm run build -w @echo/contracts && npm run test -w frontend && npm run test -w backend",
    "clean": "npm run clean -w frontend && npm run clean -w backend",
    "format": "prettier --write \"**/*.{ts,tsx,js,json,css,md}\"",
    "release": "bash scripts/release.sh"
  },
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=11.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "prettier": "^3.3.0",
    "typescript": "^5.5.0"
  },
  "optionalDependencies": {
    "@esbuild/linux-x64": "0.28.1",
    "@rolldown/binding-linux-x64-gnu": "1.1.5",
    "@unrs/resolver-binding-linux-x64-gnu": "1.12.2",
    "lightningcss-linux-x64-gnu": "1.33.0"
  }
}
