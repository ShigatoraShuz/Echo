# Registration policy documents

These Markdown files mirror the expanded, architecture-reconciled policy set introduced by migration `20260904010000_registration_onboarding_public_ownership.sql` (version `2026-09-04.1`). They are product drafts, not legal or clinical certification.

At runtime, the signup reader loads the three active records from User Service. A user must explicitly review all three current document IDs. Optional journal analysis remains a separate choice and can be declined without blocking ordinary account creation or journaling.

After a migration has been applied, never change the meaning of an existing policy version. Publish revised text through a new version and a forward-only migration, then let the access gate require review of that new active set.

The canonical runtime text is the migration/database record. Keep these review copies aligned whenever a new version is prepared.
